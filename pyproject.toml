[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "htm-state"
version = "0.0.0"
description = "HTM-State: stateful anomaly detection utilities"
readme = "README.md"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["htm_state"]
